// Operaçõesw ternários

// fazemos com if assim 

let n = 10

if (!(n%2)) {
    console.log('par')
} else {
    console.log('impar')
}

// com ternario fica

let res = !(n % 2) ? "Par!" : "Impar"

console.log(res)

// 0 equivale a false e 1 true

if (n%2 == 0) {
    console.log('PAR!!!')
} else {
    console.log('Impar!!!')
}